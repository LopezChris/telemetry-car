import React from 'react';

class PlayVideoFromImages extends React.Component {
    render() {
        const sensor_src = "../../assets/img/sensors/" + this.props.sensor_dir + "/";
        return (
            <div>

            </div>
        );
    }
}

export default PlayVideoFromImages;